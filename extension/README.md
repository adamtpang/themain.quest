# the main quest — Chrome extension

A pinnable toolbar countdown, lifeleft style. The badge on the icon always shows your years of the game remaining (no click needed), refreshed daily. Click it to glance the full life-left picture (days remaining, percent of the game spent, a rotating identity), then hit "play the game" to open the full board at themain.quest.

Everything is computed locally from your birthdate, so it works offline. The only permission is `alarms` (the daily badge refresh).

## Install (load unpacked)

1. Open `chrome://extensions` in Chrome (or Edge, Brave, Arc, any Chromium browser).
2. Turn on **Developer mode** (top right).
3. Click **Load unpacked**.
4. Select this `extension` folder.
5. Click the puzzle-piece icon in the toolbar, then the pin next to "the main quest" to keep it visible.

Already loaded an older version? Hit the reload arrow on the extension card in `chrome://extensions` to pick up the badge.

## Files

- `manifest.json` — Manifest V3, `alarms` permission only.
- `background.js` — paints the years-left badge on the icon and refreshes it daily.
- `popup.html` / `popup.js` — the life-left glance and the play button.
- `icon16.png` / `icon48.png` / `icon128.png` — the pixel heart, regenerated by `node gen-icons.mjs`.

## Publishing to the Chrome Web Store (optional, later)

Load-unpacked works for you today. To make it installable by others you would zip this folder, pay the one-time $5 Chrome Web Store developer fee, and submit it for review. Not needed for personal use.
